# @package _global_
defaults:
  - /trainer: default
  - /dataset: listops
  - /optimizer: adamw
  - /scheduler:
    - train_plateau
    - constant_w_cooldown

experiment_name: ???

callback:
  early_stopping:
    patience: 20

trainer:
  max_epochs: 120
  max_time: "00:24:00:00"

# scheduler:
#   cosine_decay_w_warmup:
#     _type_: sequential

#     interval: step
#     frequency: 1

#     scheduler_params:
#       schedulers:
#         - _type_: linear
#           step_ratio: 0.05
#           scheduler_params:
#             start_factor: 1.0e-4
#             end_factor: 1.0
#         - _type_: cosine
#           step_ratio: 0.95
#           scheduler_params:
#             T_0: 20
#             T_mult: 1
#             eta_min: 1.0e-7