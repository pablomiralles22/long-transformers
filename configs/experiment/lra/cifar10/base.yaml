# @package _global_
defaults:
  - /trainer: default
  - /dataset: cifar10
  - /optimizer: adamw
  - /scheduler: plateau

experiment_name: ???

callback:
  early_stopping:
    patience: 20

trainer:
  max_epochs: 200