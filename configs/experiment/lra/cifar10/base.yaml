# @package _global_
defaults:
  - /trainer: default
  - /dataset: cifar10
  - /optimizer: adamw
  - /scheduler: linear_decay_w_warmup

experiment_name: ???

scheduler:
  ratio_warmup_steps: 0.05

callback:
  early_stopping:
    patience: 20