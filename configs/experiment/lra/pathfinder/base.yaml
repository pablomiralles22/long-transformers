# @package _global_
defaults:
  - /trainer: default
  - /dataset: pathfinder
  - /optimizer: adamw
  - /scheduler:
    - train_plateau
    - constant_w_cooldown

experiment_name: ???

callback:
  early_stopping:
    patience: 20

trainer:
  max_epochs: 100